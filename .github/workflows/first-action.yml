name: first-workflow
on: workflow_dispatch
jobs:
 firt-job:
  runs-on: ubuntu-latest
  steps:
   - name: Print greetings
     run: echo "Hello World"
   - name: Print goodbye
     run: echo "Done - bye"
